# 이성기 — P3 백엔드·엔진

Git 협업 가이드 §9 첫날 체크리스트 실습용 문서입니다.

## 담당

P3 백엔드·엔진 — SQLite, 점수 계산, API adapter, 테스트

## 8/23까지 채울 것

레포를 확인해 보니 `domain/scoring.py`와 `domain/branching.py`는 조장님이 8/17에 이미
골격을 잡아 두셨고 테스트 6개도 통과합니다. 그래서 P3에서 아직 비어 있는 세 가지를 맡겠습니다.

| 항목 | 현재 | 할 일 |
|---|---|---|
| SQLite 저장 계층 | 없음 | PRD §12 ERD대로 스키마·seed·저장소 계층 추가 |
| API 장애 대응 | 스냅샷 fallback 있음 | 캐시·타임아웃·호출 로그·예외 처리 보강 |
| 회귀 테스트 | 없음 | 대표 사용자 3명 고정 응답으로 결과 고정 |

가이드 §5에 "tests/ 대표 사용자 결과 회귀 테스트 — 엔진 담당, PR마다 실행"으로 적혀 있어
세 번째를 특히 우선하겠습니다.

## 작업 방식

- `data/` 아래 파일은 P2 담당 영역이라 제가 고치지 않습니다. 읽기만 합니다.
- `domain/scoring.py`의 계산식도 건드리지 않습니다. 회귀 테스트는 지금 동작을 그대로 고정합니다.
- 12축(RIASEC 6 + 역량 6)과 8개 직군(J01 경영·사무·금융 ~ J08 서비스·운영)을 기준으로 맞추겠습니다.

## 연락

막히면 단체방에 먼저 공유하겠습니다.
